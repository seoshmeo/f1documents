# Исправление Larnaka Scraper - Краткая инструкция

## ✅ Что исправлено

1. **Chat ID работает!** `-1001974716718` - правильный ID, сообщения доходят до группы
2. **Claude summarizer теперь поддерживает Larnaka события** и будет генерировать описания на русском языке

## ❌ Текущая проблема

Сообщения приходят на греческом языке вместо русского, потому что:
- Claude AI не генерирует саммари (нет ANTHROPIC_API_KEY или он не настроен)
- Используется оригинальное описание с сайта (на греческом)

## 🔧 Что нужно сделать на сервере

### Вариант 1: С Claude AI (Рекомендуется)

```bash
# 1. Подключитесь к серверу
ssh root@161.35.157.202

# 2. Перейдите в директорию
cd /opt/f1documents

# 3. Получите последние изменения
git pull origin main

# 4. Откройте .env
nano .env

# 5. Убедитесь что настроено:
LARNAKA_TELEGRAM_CHAT_ID=-1001974716718
ANTHROPIC_API_KEY=ваш_anthropic_api_ключ

# 6. Сохраните (Ctrl+X, Y, Enter)

# 7. Протестируйте (опционально)
/opt/f1documents/venv/bin/python test_larnaka_notification.py

# 8. Перезапустите сервис
sudo systemctl restart larnaka-scraper

# 9. Проверьте логи
sudo journalctl -u larnaka-scraper -f
```

**Где взять ANTHROPIC_API_KEY?**
1. Зарегистрируйтесь на https://console.anthropic.com
2. Перейдите в API Keys
3. Создайте новый ключ
4. Добавьте его в .env

### Вариант 2: Без Claude AI (Быстро, но без перевода)

Если не хотите использовать Claude AI, можно модифицировать код чтобы использовать простой перевод или отправлять события на английском:

```bash
# На сервере
cd /opt/f1documents
git pull origin main

# Убедитесь что chat ID правильный
nano .env
# LARNAKA_TELEGRAM_CHAT_ID=-1001974716718

# Перезапустите
sudo systemctl restart larnaka-scraper
```

В этом случае события будут приходить с оригинальным описанием (на греческом/английском).

## 📝 Как работает новый Claude Summarizer

### Для Larnaka событий:
- Определяет что это событие (по названию "Larnaka Event")
- Использует специальный промпт с инструкциями:
  - Переводить на русский
  - Делать краткое описание (3-5 предложений)
  - Писать просто и понятно
  - Указывать для кого интересно

### Для FIA документов:
- Использует технический промпт
- Анализирует регламент
- Выделяет ключевые изменения
- Указывает затронутые команды/пилоты

## 🧪 Тестирование

### Быстрый тест отправки:
```bash
/opt/f1documents/venv/bin/python test_larnaka_notification.py
```

### Полный тест со scraper:
```bash
# Удалите одно событие из БД
docker exec -it fia_postgres psql -U postgres -d fia_documents -c "DELETE FROM larnaka_events WHERE id = (SELECT id FROM larnaka_events ORDER BY created_at DESC LIMIT 1);"

# Проверьте логи scraper
sudo journalctl -u larnaka-scraper -f
```

Scraper найдет событие заново, сгенерирует русское саммари и отправит в группу.

## ✅ Ожидаемый результат

В Telegram группе вы должны увидеть сообщение вида:

```
🎭 Культурное событие в Ларнаке

📌 [Название на русском]

📅 Дата: 15 декабря 2024
🕐 Время: 19:00
📍 Место: Larnaka Municipal Theatre

📝 Описание:
Концерт классической музыки представит произведения известных композиторов...
[краткое описание на русском языке]

🔗 Подробнее
```

## 📊 Статус изменений

- ✅ Telegram chat ID исправлен: `-1001974716718`
- ✅ Claude Summarizer обновлен для поддержки Larnaka событий
- ✅ Добавлены промпты для перевода на русский
- ✅ Тестовые скрипты созданы
- ⏳ Ожидается добавление ANTHROPIC_API_KEY на сервере

## 🔗 Связанные файлы

- [claude_summarizer.py](claude_summarizer.py) - Обновлен для поддержки событий
- [test_larnaka_notification.py](test_larnaka_notification.py) - Тестовый скрипт
- [NEXT_STEPS_SERVER.md](NEXT_STEPS_SERVER.md) - Подробная инструкция
- [.env.example](.env.example) - Пример конфигурации
